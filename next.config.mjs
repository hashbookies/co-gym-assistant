/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Local images/videos from the dataset live under /images and /videos.
  // They are served as static files from /public (see README note in Phase 1).
};

export default nextConfig;
